/**
 * File:
 *   FILENAME
 * Summary:
 *   SCR Agent for reading/writing/accessing WHATEVER_THE_AGENT_DOES
 * Access:
 *   read/write/execute OR read-only OR execute-only
 * Authors:
 *   Unknown <yast2-hacker@suse.de>
 *   BETTER <WHOYOUARE@suse.de>
 * See:
 *   anyagent
 *   libscr
 *   man MANPAGE
 *   info INFOPAGE
 *   some/other/file/relative/to/doc_home/file.html
 * Example:
 *   Read(.prom)
 *   (["WHAT", "EVER", "YOU", "GET"])
 * 
 * $Id$
 *
 * SOME USEFUL INFORMATION ON THE AGENT
 */
.prom

`ag_prom(
  SCR::PromAgent()
)
