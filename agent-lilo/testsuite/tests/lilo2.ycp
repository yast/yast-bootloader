{
    // read/write test

    // get first image section name
    string str = select (SCR::Dir (.sections), 0, "label");

    list olddir = SCR::Dir (.sections);

    // change it's label
    SCR::Write (add (add (.sections, str), "label"), "\"New Label\"");
    SCR::Write (., nil);

    return [olddir, SCR::Dir (.sections)];
}
