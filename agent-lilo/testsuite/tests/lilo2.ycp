/*
 * Authors:
 *  Unknown <unknown@addres.xx>
 *
 * $Id$
 *
 */

{
    // read/write test

    // get first image section name
    string str=select(Dir(.sections), 0, "label");

    list olddir=Dir(.sections);

    // change it's label
//    Write(add(add(.sections, str), "label"), "\"New Label\"");
//    Write(., nil);

    return [olddir, Dir(.sections)];
}
