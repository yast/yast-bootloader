/*
 * Authors:
 *  Unknown <unknown@addres.xx>
 *
 * $Id$
 *
 */

{
    // read/write test

    // get first image section name
    string str=SCR::Dir(.sections)[0]:"label";

    list olddir=SCR::Dir(.sections);

    // change it's label
//    Write(add(add(.sections, str), "label"), "\"New Label\"");
//    Write(., nil);

    return [olddir, SCR::Dir(.sections)];
}
