/*
 * Authors:
 *  Unknown <unknown@addres.xx>
 *
 * $Id$
 *
 * Test the fix to bug 19181
 */

{
    path xname = ."\"obskurni nazev\\\"\\\\\"";
    path pname = .sections + xname + .label;
    return [
	SCR::Read (.append),
	SCR::Write  (.append, SCR::Read (.append)),
	SCR::Read (.append),

	SCR::Dir (.sections),
	// rename an existing section
	SCR::Write (pname, SCR::Read (pname)),
	SCR::Dir (.sections),

	// write a new section
	SCR::Write (.sections."\"another fine\tsection\"".image, "vmtux"),
	SCR::Dir (.sections),
	];
}
